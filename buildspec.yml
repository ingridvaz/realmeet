version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - mvn clean package
artifacts:
  files:
    #- service/target/*.jar
    - service-0.0.1-SNAPSHOT.jar
cache:
  paths:
    - '/root/.m2/**/*'

