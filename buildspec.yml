version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Running tests `date`
      - mvn test
  post_build:
    commands:
      - echo Building project started on `date`
      - mvn install package
artifacts:
  files:
    - target/*.jar
cache:
  paths:
    - '/root/.m2/**/*'

