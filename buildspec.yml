version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - echo Running tests `date`
      - mvn test
  post_build:
    commands:
      - echo Building project started on `date`
      - mvn install package
artifacts:
  files:
    - '**/*'
cache:
  paths:
    - '/root/.m2/**/*'


